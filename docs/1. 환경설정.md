# Next.js 앱 생성
npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*"

# shadcn/ui
npx shadcn@latest init
npx shadcn@latest add button input card dialog textarea label badge scroll-area dropdown-menu alert-dialog sheet tabs avatar

# DB + ORM
npm install drizzle-orm better-sqlite3
npm install -D drizzle-kit @types/better-sqlite3

# 드래그앤드롭
npm install @dnd-kit/core @dnd-kit/sortable @dnd-kit/utilities

# 폼 + 유틸
npm install react-hook-form @hookform/resolvers zod uuid
npm install -D @types/uuid

# 인증 (추가)
npm install next-auth @auth/drizzle-adapter

# 폴더 생성
mkdir specs tests data